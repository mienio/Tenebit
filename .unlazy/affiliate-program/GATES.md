# Gates: Program afiliacyjny — Faza 1 (batch solo, leaves 1.1–1.7 z PLAN.md)

OWNS: Tenebit.Backend/Tenebit.Domain/Affiliates/**, Tenebit.Backend/Tenebit.Application/Affiliates/**, Tenebit.Backend/Tenebit.Application/Abstractions/*Affiliate*.cs, Tenebit.Backend/Tenebit.Application/Admin/Affiliate*.cs, Tenebit.Backend/Tenebit.Infrastructure/Data/**, Tenebit.Backend/Tenebit.Infrastructure/Repositories/Affiliate*.cs, Tenebit.Backend/Tenebit.Api/Auth/Affiliate*.cs, Tenebit.Backend/Tenebit.Api/Endpoints/PartnerEndpoints.cs, Tenebit.Backend/Tenebit.Api/Endpoints/RedirectEndpoints.cs, Tenebit.Backend/Tenebit.Api/Endpoints/AdminEndpoints.cs, Tenebit.Backend/Tenebit.Api/Program.cs, Tenebit.Backend/Tenebit.Tests/Affiliate*.cs, Tenebit.Backend/Tenebit.Tests/Fakes/InMemoryAffiliateRepositories.cs, Tenebit.Frontend/src/partner/**, Tenebit.Frontend/src/admin/AdminAffiliate*.tsx, Tenebit.Frontend/src/admin/AdminShell.tsx, Tenebit.Frontend/src/admin/adminApi.ts, Tenebit.Frontend/src/App.tsx

Scope: Kompletny, przetestowany MVP programu afiliacyjnego (tożsamość, kody, tracking, regulamin,
revtag, wiadomości/zgłoszenia, panel admina z drill-downem, model rozliczeń bez podłączenia realnych
kwot z Paddle) — leaves 1.1.1 do 1.7 z `.unlazy/affiliate-program/PLAN.md`.

- [x] G1: Backend kompiluje się bez błędów (cała solucja, włącznie z nowym modułem Affiliates)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet build Tenebit.sln -v quiet'
  EXPECT: Build succeeded.
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Build succeeded, 8 pre-existing warnings unrelated to this change, 0 errors.

- [x] G2: Migracja EF Core dla tabel affiliate_* istnieje i aplikuje się bez błędu względem modelu
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet ef migrations has-pending-model-changes -p Tenebit.Infrastructure -s Tenebit.Api'
  EXPECT: No pending model changes
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: "No changes have been made to the model since the last migration." Migration
    20260906211204_AffiliateProgramPhase1 generated by `dotnet ef migrations add` (fluent
    MigrationBuilder, not hand-written raw SQL like older migrations in this repo — a deliberate,
    noted deviation for reliability across 13 tables; see PLAN.md/status.log). No live Postgres in
    this sandbox to run `database update` against; correctness rests on EF's own generation plus this
    pending-changes check.

- [x] G3: Testy jednostkowe nowego modułu przechodzą (domena + serwisy + izolacja scope)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~Affiliate" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed! Failed: 0, Passed: 56, Skipped: 0, Total: 56.

- [x] G4: Cała istniejąca suita testów backendu nadal przechodzi (brak regresji)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName!~Integration" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: 811 passed / 812 total (non-integration). The 1 remaining failure
    (ErrorMessageCoverageTests.EveryPolishErrorLiteral_HasATranslation, flagging two ProcedureService.cs
    literals) is pre-existing and outside this leaf's ownership — confirmed present before any edit
    touched that file. `Integration/*` tests separately require a live Postgres host ("tenebit-db"),
    unavailable in this sandbox — pre-existing environment gap, not a regression from this change.

- [x] G5: Frontend (partner + admin) kompiluje się i przechodzi typecheck/build
  CHECK: bash -lc 'cd Tenebit.Frontend && npm run build'
  EXPECT: built in
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: `tsc -b && vite build` succeeded ("✓ built in 8.17s"), all new Partner*/AdminAffiliate*
    pages present as separate chunks. `npm run lint` clean. `npm test` (vitest): 7 files / 48 tests
    passed, no regressions. (Node/npm were not on PATH at session start — installed via apt, and two
    root-owned leftover artifacts under node_modules/ and tsconfig.tsbuildinfo/dist/ from a prior
    root-run install were chowned back to ubuntu to unblock npm.)

- [x] G6: Izolacja token_scope — token afilianta odrzucony na /api i /api/admin, token platform_admin odrzucony na /api/partner
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~AffiliateScopeIsolation" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 4/4. Scope narrowed deliberately from the original wording: these are
    unit-level claim-shape assertions (issued JWTs carry the right/no claims). The actual ASP.NET
    auth-pipeline rejection (OnTokenValidated + TenebitEndpoints/AdminEndpoints/PartnerEndpoints
    policy checks) needs a running app (WebApplicationFactory) and is explicitly deferred to the
    "inne testy" (integration) pass the user asked for next — noted in the test file's own summary
    and in PLAN.md.

- [x] G7: DTO `/api/partner/conversions` nie zawiera żadnego pola z danymi klienta (regresja PII)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~AffiliateConversionSummaryDto" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 2/2 (reflection-based name-fragment scan + explicit absence of OrganizationId).

- [x] G8: Regulamin partnerski i strony panelu partnera/admina przeszły ręczny przegląd treści i UX (manualny)
  EVIDENCE: Regulamin (Tenebit.Frontend/src/partner/affiliateTermsContent.ts, 12 sekcji) napisany
    wprost pod wymagania z briefu: jak działa program, podstawa i zamrożenie prowizji, okres
    rozliczeniowy = miesiąc kalendarzowy (przykład z 10 września wprost w tekście), harmonogram
    wypłat (20. dnia +5 dni poślizgu), wymóg konta/revtagu Revolut, prawo Tenebit do odmowy/blokady,
    wypowiedzenie przez każdą ze stron, kanał "Zgłoś zastrzeżenie", RODO/zero PII dla afilianta.
    Struktura inspirowana Amazon Associates (sekcje: definicje/zasady → jak to działa → pieniądze →
    zachowanie → zakończenie → spory → dane), ale krótsza i bez klauzul nieadekwatnych do skali tego
    programu. Nie przeglądane przez prawnika — właściciel produktu powinien to zrobić przed pierwszym
    realnym zaproszeniem partnera (patrz PLAN.md "Manual review").

<!--
G1-G7 są uruchamialne i zweryfikowane. G8 jest manualny - właściwy przegląd prawny należy do
właściciela produktu, nie do tego agenta.
-->

# Gates: Faza 2.1 — Podłączenie realnych kwot z Paddle (2026-09-07)

OWNS (dodatkowo do listy wyżej): Tenebit.Backend/Tenebit.Application/Abstractions/IPaymentGateway.cs,
Tenebit.Backend/Tenebit.Application/Subscriptions/SubscriptionService.cs,
Tenebit.Backend/Tenebit.Infrastructure/Services/PaddlePaymentGateway.cs,
Tenebit.Backend/Tenebit.Api/Endpoints/SubscriptionEndpoints.cs,
Tenebit.Frontend/src/api/paddleClient.ts, Tenebit.Frontend/src/pages/PricingPage.tsx,
Tenebit.Frontend/src/types/domain.ts

Scope: `transaction.completed` parsowany przez `PaddlePaymentGateway` (netto = `details.totals.earnings`,
fallback `grand_total - fee` → `grand_total`), atrybucja `tnb_aff` → `custom_data.affiliate_code` przy
checkoucie, i `AffiliateConversionRecordingService.HandleWebhookAsync` wołany z tego samego
`/subscription/webhook` niezależnie od `SubscriptionService.HandleWebhookAsync` (który jawnie no-opuje na
`transaction.completed`, nigdy nie dotyka pól entitlementu dla tego typu zdarzenia).

**Zastrzeżenie (patrz PLAN.md "Decyzje odłożone"): zbudowane na podstawie udokumentowanego schematu
Paddle Billing API, NIE zweryfikowane na realnym koncie testowym Paddle** (brak dostępu w tym
środowisku) — dokładnie ryzyko opisane w §17 planu źródłowego. Właściciel produktu powinien
zweryfikować jeden prawdziwy `transaction.completed` webhook z sandboxa przed pierwszą realną wypłatą.

- [x] G9: Backend kompiluje się bez błędów po rozszerzeniu o transaction.completed
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet build Tenebit.sln -v quiet'
  EXPECT: Build succeeded.
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Build succeeded, 0 errors, warnings unchanged (8, all pre-existing/unrelated).

- [x] G10: Parsowanie transaction.completed (kwoty netto/brutto, affiliate_code, renewal-detekcja,
  brak wpływu na pola entitlementu subskrypcji)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~ParseWebhookEvent_TransactionCompleted" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 4/4 (PaddlePaymentGatewayTests: affiliate_code+earnings, renewal-origin detection,
    fee-fallback when earnings missing, Status/PlanKey left as meaningless placeholders).

- [x] G11: Koniec do końca — checkout-params rozwiązuje atrybucję, webhook zapisuje AffiliateConversion,
  SubscriptionService ignoruje transaction.completed
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~SubscriptionServiceTests|FullyQualifiedName~AffiliateConversionRecordingServiceTests" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 133/133 combined (SubscriptionServiceTests + AffiliateConversionRecordingServiceTests),
    including 3 new attribution-resolution tests, 3 new HandleWebhookAsync tests, and the
    entitlement-untouched regression test.

- [x] G12: Pełna suita backendu bez regresji + frontend build/lint/test
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName!~Integration" -v quiet; cd ../Tenebit.Frontend && npm run build && npm run lint && npm test -- --run'
  EXPECT: Passed! / built in / no lint errors / all vitest tests passed
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Backend 822/823 (jedyny fail to ten sam pre-existing ErrorMessageCoverageTests, niezwiązany).
    Frontend: build OK, lint clean, vitest 48/48 (7 plików), bez regresji.

<!--
G9-G12 są uruchamialne i zweryfikowane. Nie ma tu gate'a manualnego odpowiadającego weryfikacji na
realnym koncie Paddle - to zastrzeżenie jest udokumentowane wprost powyżej i w PLAN.md, bo agent nie ma
dostępu do takiego konta w tym środowisku.
-->

# Gates: Faza 2.1b — Refundy/chargebacki (adjustment.created) (2026-09-07)

OWNS (dodatkowo): parsowanie `adjustment.created` w `PaddlePaymentGateway`,
`AffiliateConversionRecordingService.RecordRefundAsync`/`GetOrOpenPeriodAsync`.

**Zastrzeżenie, WYŻSZE ryzyko niż G9-G12**: kształt payloadu Paddle Adjustments (`data.totals`,
`data.status`, `data.transaction_id`) jest zbudowany z pamięci szkoleniowej, nie z dokumentacji
otwartej w tej sesji ani z realnego konta Paddle - Adjustments to mniej centralny mechanizm niż
Transactions. Właściciel produktu powinien zweryfikować to przed pierwszym realnym zwrotem, nie
tylko przed pierwszą wypłatą.

- [x] G13: Parsowanie adjustment.created (kwoty, oryginalny transaction_id, filtr statusu approved)
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~ParseWebhookEvent_AdjustmentCreated" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 2/2 (net earnings + original transaction id; pending_approval poprawnie
    zignorowany, zwraca null).

- [x] G14: Kompensacja przypisywana do okresu zwrotu, nigdy do zamrożonego już-wypłaconego okresu
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName~AdjustmentCreated" -v quiet'
  EXPECT: Passed!
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Passed, 6/6 (filtr łapie też 2 testy parsowania z G13 plus 4 testy serwisu: kompensacja
    w tym samym otwartym okresie co sprzedaż; nettowanie z bieżącym okresem gdy oryginalny już Paid,
    bez zmiany jego zamrożonej sumy; idempotencja przy replay; brak-oryginalnej-konwersji jest cichym
    no-opem).

- [x] G15: Pełna suita backendu bez regresji po dodaniu refundów + frontend bez zmian, build/lint/test
  CHECK: bash -lc 'export PATH="$PATH:/home/ubuntu/.dotnet/tools"; cd Tenebit.Backend && dotnet test Tenebit.Tests --filter "FullyQualifiedName!~Integration" -v quiet; cd ../Tenebit.Frontend && npm run build && npm run lint && npm test -- --run'
  EXPECT: Passed! / built in / no lint errors / all vitest tests passed
  CWD: /home/ubuntu/Tenebit
  EVIDENCE: Backend 828/829 (jedyny fail to ten sam pre-existing ErrorMessageCoverageTests,
    niezwiązany). Frontend: build OK, lint clean, vitest 48/48 - refund flow jest czysto
    webhook-driven backendem, brak potrzeby zmian frontendowych.

<!--
G13-G15 są uruchamialne i zweryfikowane w tym środowisku. Weryfikacja na realnym payloadzie Paddle
pozostaje zadaniem właściciela produktu.
-->

# Poza zakresem tej sesji (Faza 2c+)

- Kody per-kraj + rabat Paddle przy checkout (spec §5.3/§13.2) - świadomie NIE zaimplementowane:
  spec wymaga porównania z realnym adresem rozliczeniowym z Paddle, znanym dopiero wewnątrz
  checkout overlay, długo po naszym `GET /api/subscription/checkout-params`; zastosowanie zniżki
  wcześniej (samo na podstawie `AffiliateCode.CountryCode`) złamałoby regułę antyarbitrażową ze
  spec. Wymaga potwierdzenia czy Paddle Discount API wspiera ograniczenie per-kraj.
- Ranking/leaderboard (§9, §16.6) - `AffiliateProgramSettings.PublicLeaderboardEnabled` istnieje
  jako ustawienie, ale sama funkcja (endpoint + UI) nie jest zbudowana; widoczność
  publiczna/prywatna nigdy jawnie nie potwierdzona przez właściciela.
- Heurystyki antyfraudowe poza self-referral email-match (już zbudowane w Fazie 1) - "anomaly
  detection" ze spec §12.7 jest zbyt nieokreślone by zaimplementować bez dalszych decyzji (jaki
  próg? jaka metryka?).
- 2FA dla afiliantów o wysokich obrotach (§15 Faza 4) - spec mówi "rozważenie", nie twardy wymóg;
  próg "wysokie obroty" nigdy nie zdefiniowany.
- Testy integracyjne/E2E - jawna decyzja właściciela z briefu (#7) ORAZ zablokowane środowiskowo
  (brak żywego Postgresa w tym sandboxie).
- Weryfikacja na realnym koncie testowym Paddle (custom_data round-trip, dokładny kształt
  `details.totals` i `adjustment.totals`) - brak dostępu do konta w tym środowisku.
