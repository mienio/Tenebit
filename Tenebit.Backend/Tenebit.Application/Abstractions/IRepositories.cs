namespace Tenebit.Application.Abstractions;

// Repository contracts were split into one declaration per file. This compatibility file is kept
// intentionally empty so patch-based deployments do not need to delete the old source file.
